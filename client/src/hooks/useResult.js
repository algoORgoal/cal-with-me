// import { useState } from 'react';
// import useExpression from '../useExpression'

// const useResult = () => {
// 	const [result, setResult] = useResult(null);
	
// 	const calculate = (expression) => 

	// const add = (num) => setResult((prevResult) => prevResult + num);

	// const subtract = (num) => setResult((prevResult) => prevResult - num);

	// const multiply = (num) => setResult((prevResult) => prevResult * num);

	// const divide = (num) => setResult((prevResult) => prevResult / num);

	// return { result, add, subtract, multiply, divide };
};